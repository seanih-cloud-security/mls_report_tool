[project]
name = "mls-report-automation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "seanih", email = "sean.anih@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
]

[project.scripts]
mls-report-automation = "mls_report_automation:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
